# Dependencies
node_modules/

# Build output
# dist/ IS committed: the Hostinger "Express" deployment installs dependencies
# and starts the entry file — it does not build. Run `npm run build` before
# pushing so the served assets match the source.
out/
build/
.next/

# Environment / secrets
.env
.env.local

# Local data + uploads (runtime, not source)
data.sqlite
data.sqlite-shm
data.sqlite-wal
uploads/*
!uploads/.gitkeep

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
