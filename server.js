// Entry point for the Hostinger "Express" deployment, whose default startup
// file is server.js at the application root. The application itself lives in
// server/index.js; this keeps the platform default working so the deployment
// setting needs no custom value.
import './server/index.js';
